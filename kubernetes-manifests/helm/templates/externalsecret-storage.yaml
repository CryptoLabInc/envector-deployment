{{- /* Creates ExternalSecret for storage credentials when External Secrets Operator is enabled */ -}}
{{- if and .Values.externalSecrets.storageSecret.enabled (gt (len .Values.externalSecrets.storageSecret.data) 0) }}
{{- $defaultSecretName := printf "%s-storage-eso" (include "es2-chart.fullname" .) }}
{{- $secretName := (default $defaultSecretName .Values.externalSecrets.storageSecret.name) }}
{{- $storeName := required "externalSecrets.storageSecret.secretStoreRef.name or externalSecrets.secretStoreRef.name is required when externalSecrets.storageSecret.enabled=true" (coalesce .Values.externalSecrets.storageSecret.secretStoreRef.name .Values.externalSecrets.secretStoreRef.name) }}
{{- $storeKind := default "ClusterSecretStore" (coalesce .Values.externalSecrets.storageSecret.secretStoreRef.kind .Values.externalSecrets.secretStoreRef.kind) }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ $secretName }}
  labels:
    app: {{ include "es2-chart.fullname" . }}
    component: eso-storage
spec:
  refreshInterval: {{ .Values.externalSecrets.storageSecret.refreshInterval | default "1m" | quote }}
  secretStoreRef:
    name: {{ $storeName | quote }}
    kind: {{ $storeKind | quote }}
  target:
    name: {{ $secretName }}
    creationPolicy: Owner
  data:
    {{- range .Values.externalSecrets.storageSecret.data }}
    - secretKey: {{ .secretKey | quote }}
      remoteRef:
        key: {{ .remoteRef.key | quote }}
        {{- if .remoteRef.property }}
        property: {{ .remoteRef.property | quote }}
        {{- end }}
    {{- end }}
{{- end }}
